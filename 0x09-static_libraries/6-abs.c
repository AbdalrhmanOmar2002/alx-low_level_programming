#include "main.h"

/**
 * _abs - that computes the absolute value of an integer.
 *
 * @j: takes in integer type input for function
 *
 * Return: Always 0 (Success)
 */

int _abs(int j)
{
if (j < 0)
j = (-1) * j;
return (j);
}
